name: Deploy Backend to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build and push Backend image
        uses: docker/build-push-action@v3
        with:
          context: .
          push: true
          tags: |
            tylerpham2708/okr-kpi-system-server:latest
            tylerpham2708/okr-kpi-system-server:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Copy docker-compose.yml to VPS
        uses: appleboy/scp-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          source: "docker-compose.yml"
          target: "~/okr-kpi-system/okr-kpi-system-be/"

      - name: Executing remote SSH commands to deploy
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SSH_KEY }}
          envs: |
            IMAGE_TAG=${{ github.sha }}
          script: |
            cd ~/okr-kpi-system/okr-kpi-system-be/
            env IMAGE_TAG=$IMAGE_TAG $(grep -v '^#' .env | xargs) docker stack deploy --with-registry-auth -c docker-compose.yml okr_kpi_system_server
            docker image prune -f